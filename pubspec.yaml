name: xmtp
description: XMTP client SDK for Flutter applications.
# Version replaced at build time in release.yml
version: 0.0.0
repository: https://github.com/xmtp/xmtp-flutter

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: '>=3.7.0'

dependencies:
  async: ^2.10.0
  grpc: ^3.1.0
  cryptography: ^2.5.0
  cryptography_flutter: ^2.3.0
  fixnum: ^1.1.0
  flutter:
    sdk: flutter
  meta: ^1.9.0
  path: ^1.8.3
  path_provider: ^2.1.2
  pointycastle: ^3.6.2
  quiver: ^3.2.1
  web3dart: ^2.6.1
  xmtp_proto: ^3.33.0
#  xmtp_bindings_flutter: ^0.1.0-development.3
  xmtp_bindings_flutter:
    path: ../libxmtp/bindings_flutter

dependency_overrides:
  stream_channel: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  mocktail: ^0.3.0

flutter:
